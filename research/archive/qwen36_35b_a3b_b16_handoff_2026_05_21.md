# B16 On-Device 35B-A3B Server — Handoff (updated 2026-05-21, late evening)

## Where things stand

🎯 **MILESTONE REACHED 2026-05-21:** the FULL forward chain runs on
device. `step_forward_ttnn` in `experiments/serve/server_35b_ttnn.py`
does embed → 40 layers → final_norm → lm_head → argmax with NO host
↔ device data movement in the inner loop beyond a single 8-byte
argmax readback per token. Commit `fbd52a0`.

End-to-end smoke (qb1, fresh tt-smi reset):
```
caches reset: 30 DN, 40 KV placeholders
next_id=92546 text='ythe'  (took 803.5 ms post-warmup)
✓ FULL on-device step_forward_ttnn works end-to-end
```

Output is gibberish ('ythe' at token 0) — see "remaining placeholders"
below. Plumbing complete; correctness work is what's left.

## Commits since handoff-prev (was B16g)

| Commit | What |
|---|---|
| `cc896f8` B16h | `step_forward_ttnn` skeleton (embed + 40-L loop + lm_head + argmax) + `State.reset_caches_ttnn()` for DN cache pre-allocation. |
| `fbd52a0` B16i | End-to-end smoke passes. Fix: argmax requires ROW_MAJOR layout (TILE→ROW_MAJOR via `ttnn.to_layout` before `ttnn.argmax`). |
| `777a9e3` B16j | DN L2-norm on Q/K (rsqrt(sum(x*x, -1, keepdim) + EPS) form) + GQA 4→8 head repeat_interleave via reshape→repeat→reshape (replaces the wrong concat order). |
| `89f974c` B16k | DN conv1d kernel weight at current position (token-0 collapses to `mixed_qkv * w[:, kernel_size-1]`). |

## Smoke results (current, qb1)

```
MoE  out norm 1.4854 ✓ (routed + shared, full math — unchanged)
DN   out norm 0.2456 ✓ (L2-norm + interleave; conv1d w[:, k-1] only)
Attn out norm 0.1122 ✓ (projections + gate + o_proj; RoPE/KV placeholder)
Layer 0 out norm 1.7001 ✓ (composed)
step_forward end-to-end: 803 ms/tok, next_id=92546 ('ythe')
```

## Numpy server still ships coherent text

`server_35b.py` (B14, commit `f8bce3a`) generates 'The capital of France is
Paris, a city renowned for its iconic landmarks…' at 3.44 tok/s. Use this
as the production fallback while B16 ttnn server completes.

## Remaining placeholders (correctness gap to coherent ' Paris')

| Block | Placeholder | Impact | Fix size |
|---|---|---|---|
| DN | conv1d state-shift across tokens (only kernel[k-1] applied) | wrong at pos ≥ 1 | 30-50 lines (in-place ring on conv_state_tt) |
| Attn | RoPE: cos/sin tensors passed but unused inside attn_forward_ttnn | identity at pos 0, breaks pos ≥ 1 | 20 lines (slice rotary_dim, apply rotation, concat back) |
| Attn | KV cache: never updated, never read | single-token attention is identity (pseudo-ok at pos 0), no historical attention | 50 lines (per-layer pre-alloc + append per token; in-place mesh op) |
| Attn | GQA chip→KV-head mapping | currently uses first KV head broadcast; should be `chip // (NCHIPS // NUM_KV_HEADS)` | 10 lines |

For token 0 specifically, the conv1d, RoPE, and KV-cache placeholders
collapse to approximately-correct math (zero state, identity rotation,
empty cache). The GQA chip mapping is wrong even at token 0. So
single-token cosine vs the numpy oracle would isolate the GQA fix as
the only first-token correctness gap; the others need multi-token
validation.

## Recommended next steps (user picks)

Three reasonable paths surfaced at the end of the active session;
explicit user decision pending:

**(a)** Keep iterating placeholders + build a numpy-oracle cosine probe
in parallel — proves layer-by-layer math against `server_35b.py`.
Standard B.2.2 playbook from 27B bringup. Estimated 6-10 iterations
(~3 hours) to coherent ' Paris'.

**(b)** Jump straight to trace capture (B17) on the current
placeholder-correct path — harvests perf win first (1.8s → likely
~100-200ms/tok), then comes back for correctness. Risks: trace
capture may not survive the in-place ops needed for KV cache /
conv_state evolution, so we'd partly re-do this work.

**(c)** Validate single-token correctness end-to-end first (numpy
oracle probe at layer 0 output cosine) before any more on-device
edits. Tightest evidence trail.

Resume command (qb1):
```bash
rsync -avz experiments/serve/server_35b_ttnn.py \
  qb1:/home/aditya/tt-xla/experiments/serve/server_35b_ttnn.py
ssh qb1 'cd /home/aditya/tt-xla && tt-smi -r 2>&1 | tail -1 && \
  export TT_METAL_HOME=$HOME/tenstorrent/tt-metal && \
  export TT_BUILD_DIR=$TT_METAL_HOME/build_Release && \
  export ARCH_NAME=blackhole && \
  export PYTHONPATH=$TT_METAL_HOME/ttnn:$PYTHONPATH && \
  export LD_LIBRARY_PATH=$TT_METAL_HOME/ttnn/ttnn:$TT_BUILD_DIR/ttnn:$TT_BUILD_DIR/lib:$LD_LIBRARY_PATH && \
  timeout 400 .venv/bin/python -u experiments/serve/server_35b_ttnn.py 2>&1 | tail -40'
```

Bootstrap is 107s one-time (40 layers); subsequent iterations ~3 min
per smoke cycle.

## Iteration cost

- Bootstrap: 107s (weight upload, one-time per process)
- Smoke run (post-bootstrap): 800 ms
- Total per code-edit cycle: ~3 min (rsync + tt-smi + bootstrap + smoke)

## Files

- `experiments/serve/server_35b.py` — B14 numpy server (PRODUCTION fallback)
- `experiments/serve/server_35b_ttnn.py` — on-device server (B16a-k, WIP)
- `experiments/serve/scripts/serve_35b.sh` — wrapper for numpy server
- `experiments/serve/client_35b.py` — client for numpy server
- `experiments/utils/needle_haystack_35b.py` — long-context probe
- `experiments/utils/profile_35b_forward.py` — numpy per-block profiler

## Reference patterns (don't re-derive)

- Mesh tensor ranks: ttnn.matmul output is rank 3 (per-chip view padded).
  ShardTensorToMesh tensors have logical shape with leading 1 in mesh dim
  (NOT NCHIPS). Slice begins/ends must match tensor rank — use len(shape)
  to detect.
- ttnn.all_reduce: only `cluster_axis` kwarg (sum is default).
- ttnn.to_torch: needs `mesh_composer=ttnn.ConcatMeshToTensor(mesh, dim=0)`
  for mesh tensors; take [0] of result for replicated tensors.
- (1+w) layernorm convention: pre-add 1.0 at upload to use stock
  ttnn.rms_norm. Applied to `input_layernorm` + `post_attention_layernorm`
  weights. Not applied to `linear_attn.norm.weight` (Qwen3_5MoeRMSNormGated
  uses bare w).
- Expert table format: per-chip `experts_gate_up` [256, HIDDEN, INTERMEDIATE]
  (TRANSPOSED from HF [256, INTERMEDIATE, HIDDEN]); `experts_down` similarly.
  Slice rank-4 begins=[0, e, 0, 0] ends=[1, e+1, INTERMEDIATE, HIDDEN].
- `ttnn.argmax(use_multicore=True)` requires ROW_MAJOR layout — convert
  from TILE via `ttnn.to_layout(t, ttnn.ROW_MAJOR_LAYOUT)` before the call.
- `ttnn.embedding` table needs `layout=ttnn.ROW_MAJOR_LAYOUT` at upload.
- `torch.from_numpy(np.uint32(...))` fails on older torch — use `np.int32`
  then cast to `ttnn.uint32` at the `ttnn.from_torch` boundary.
- GQA `repeat_interleave` (no ttnn op) emulated via
  reshape(NK,1,D)→repeat([1,1,R,1])→reshape(NV,D).

## Git state (handoff time)

4 commits ahead of origin/main (cc896f8, fbd52a0, 777a9e3, 89f974c).
Working tree clean apart from untracked `.cache/repro_validation_2026_05_21/`.
Not pushed (user has not authorized push).
