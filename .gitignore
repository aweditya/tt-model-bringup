# Cloned repos (large, don't commit)
repos/

# Scraped raw HTML
research/raw/

# OS files
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Scraped doc corpus — regenerable via pjrt_plugin/scripts/scrape_tt_docs.py
tt_docs_corpus/
experiments/.refs/

# Tracy raw artifacts — large, regenerable via experiments/utils/qb2_tp_tracy_profile_probe.py
# (parsed summary JSONs in .cache/qb2_tp_tracy/ remain tracked)
research/probe_logs/qb2_tp_tracy_p25_sync_20260515_0446/

# Claude Code local runtime/config — per-machine, not source
.claude/

# Scratch artifacts under .cache/ — leading-underscore = local-only convention
# (e.g. large intermediate NPZs from cosine_ladder_tp runs)
.cache/**/_*

# Local environment file — never commit secrets (HF_TOKEN, etc.).
# Template lives at .env.example.
.env
